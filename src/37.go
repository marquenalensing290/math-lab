package main

import (
    "fmt"
)

func main() {
    // Define variables
    var x, y float64

    // Set initial values
    x = 2.0
    y = 3.5

    // Perform calculations
    result := x + y

    // Print the results
    fmt.Println("The sum of", x, "and", y, "is:", result)
}
